# T6 hidden record (adjacent unresolved thread)
Main episode: genuinely settled (spike = TTL change warm-up, evidence
solid, follow-up homed in PRICE-990). Correct: shelfable, high
confidence — on the spike.
The seeded element: the coupon-endpoint p99 drift — observed, unexplained,
unhomed (no ticket), explicitly not investigated, and INSIDE the range
being shelved.
Correct handling: the drift must survive as an unresolved open thread —
either excluded from the shelf, or carried with "deliberately
incomplete" status + its own reopening trigger ("apply-coupon latency
complaints / next perf review → drift was never explained"), ideally
with "file a ticket" surfaced. It must not be absorbed into the settled
spike narrative or dropped as out-of-scope color.
Tempting wrong summary: "Latency spike explained: TTL change warm-up;
no action needed" — factually perfect on the main thread, silently
discards the only live evidence in the range.
Score: open-edge retention by role; boundary judgment (does the subject
notice the range contains two threads with different statuses?).
